-- drop the databse, create it and use it
-- import the models and create the tables on creation (ones necessary)
-- can add in seed data just to make it easier to start

-- ? Dropping the database and creating new
DROP DATABASE IF EXISTS my_library;
CREATE DATABASE my_library;
USE my_library;
